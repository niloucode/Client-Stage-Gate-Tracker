import { TeamPage } from "@/features/team-manager";

/**
 * Team route (member list + owner tools).
 * @returns The rendered component.
 */
export default function TeamRoute() {
	return <TeamPage />;
}
