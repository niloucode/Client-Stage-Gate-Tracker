// Vitest stub for the `server-only` package. Its real entry throws when
// imported outside a React Server Component build; tests must resolve it to
// an empty module (aliased in vitest.config.ts).
export {};
